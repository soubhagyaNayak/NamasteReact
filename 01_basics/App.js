const heading=React.createElement("h1",{},"hello duniya");

                 //creating the root and renderening something inside the root is the work of react dom
                const root=ReactDOM.createRoot(document.getElementById("root"));
                //react code is rendered
                root.render(heading);